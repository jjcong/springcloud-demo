spring:
  cloud:
    config:
      #需要从github上读取的仓库名称，注意没有yml后缀名
      name: microservicecloud-config-client
      #本次访问的环境项
      profile: test
      label: master
      #本微服务启动后先去找3344服务，通过SpringCloudConfig获取GitHub的服务地址
      uri: http://config-3344.com:3344


